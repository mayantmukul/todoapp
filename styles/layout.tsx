import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  default: {
    borderWidth: 2,
    borderColor: 'black',
    padding: 8,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
