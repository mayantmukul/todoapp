import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  default: {
    borderWidth: 2,
    borderColor: 'black',
    padding: 16,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
