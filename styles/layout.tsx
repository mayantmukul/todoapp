import {StyleSheet} from 'react-native';

const Layout = {
  padding: {
    small: 8,
  },
};

export default StyleSheet.create({
  default: {
    //borderWidth: 2,
    //borderColor: 'black',
    padding: 8,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
