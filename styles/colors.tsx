const isDark = true;

const Colors = {
  primary: isDark ? '#1F2024' : '#6AABD2',
  secondary: isDark ? '#030000' : '#B7CFDC',
  highlight: isDark ? '#030000' : '#385E72',
  // background: isDark ? '#1F2024' : '#D9E4EC',
  background: isDark ? '#1F2024' : '#6AABD2',

  textMain: isDark ? 'white' : 'black',
  textBg: isDark ? '#343C3B' : 'white',
  textMuted: isDark ? '#A4A8AB' : '#999',
};

export default Colors;
